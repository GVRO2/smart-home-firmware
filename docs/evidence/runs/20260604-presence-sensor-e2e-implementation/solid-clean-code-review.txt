SOLID and Clean Code Review Log
===============================
Date: 2026-06-04

Review Metrics & Gates:
1. Single Responsibility Principle (SRP):
   - ESP32 side: GpioPresenceSensor deals only with physical hardware reading, and PresenceReading/PresenceSensor interfaces encapsulate abstractions cleanly.
   - Backend side: Domain model EnvironmentalMeasurement keeps validation logic, UseCase manages coordination, Controller manages HTTP mapping.
2. Open/Closed Principle (OCP):
   - Adding presence was achieved by introducing `GpioPresenceSensor` implementing the `PresenceSensor` interface, and extending `EnvironmentReading` without changing core sensing logic.
3. Liskov Substitution Principle (LSP):
   - `GpioPresenceSensor` implements `PresenceSensor` and can substitute it seamlessly in `main.cpp`.
4. Interface Segregation Principle (ISP):
   - ESP32 uses clean sensor interfaces.
5. Dependency Inversion Principle (DIP):
   - High-level classes depend on `PresenceSensor` abstraction, not `GpioPresenceSensor` directly.

Clean Code:
- No dead code or diagnostic statements committed.
- Clean separation of concerns between layers (Clean Architecture strictly respected).
- JPA Entities are kept inside persistence adapters and NOT exposed on the REST controller layers.
- Controller contains no business logic.

Result:
SOLID_CLEAN_CODE_REVIEW_PASSED (SUCCESS)
