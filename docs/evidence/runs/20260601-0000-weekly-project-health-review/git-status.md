# Git Status

## Firmware

Firmware working tree was dirty before this weekly review. The review preserved existing changes and added weekly review artifacts.

Baseline captured in `raw/firmware-git-status.txt`.

Notable pre-existing modified/untracked areas included firmware audio, governance, docs, mocks, scripts and tests.

## Backend

Backend working tree was clean when captured.

Baseline captured in `raw/backend-git-status.txt`.
