# Next Steps

1. Run `.\scripts\governance\run-firmware-approval-review.ps1 -Mode physical` with ESP32 connected.
2. Capture serial, sensor, MQTT, and audio evidence.
3. Run `.\scripts\governance\run-firmware-approval-review.ps1 -Mode release` after compatible physical evidence exists.
